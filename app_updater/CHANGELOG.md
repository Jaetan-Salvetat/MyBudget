## 0.1.0

* **Breaking**: removed the background update worker. `startBackgroundWorker()`, `stopBackgroundWorker()`, `UpdateWorker` and `NotificationConfig` are gone, along with `UpdateConfig.backgroundCheckInterval` and `UpdateConfig.notificationConfig`.
* Dropped the `workmanager` and `flutter_local_notifications` dependencies.
* Updated all dependencies to their latest versions.

## 0.0.1

* TODO: Describe initial release.
