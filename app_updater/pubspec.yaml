name: app_updater
description: "Plug-and-play update lifecycle for Android apps: detection, notification, download, and installation of APKs from GitHub Releases."
version: 0.1.0
homepage:

environment:
  sdk: ^3.11.1
  flutter: ">=3.11.1"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  path_provider: ^2.1.5
  path: ^1.8.3
  package_info_plus: ">=9.0.0 <11.0.0"
  app_installer: ^1.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.0

flutter:
