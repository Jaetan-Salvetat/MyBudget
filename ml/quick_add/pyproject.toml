[project]
name = "multi-head-v1"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "torch",
    "transformers",
    "datasets",
    "scikit-learn",
    "onnx",
    "onnxruntime",
    "accelerate",
]
