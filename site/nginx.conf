server {
    listen 80;
    root /usr/share/nginx/html;
    index privacy.html;

    location = /flags.json {
        default_type application/json;
        add_header Cache-Control "public, max-age=300";
    }
}
